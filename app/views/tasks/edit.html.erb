<h1>Edit task</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, :class => 'form-control input' %>

  <%= f.label :details %>

    <%= f.text_field :details, :class => 'form-control input' %>

  <%= f.submit :class => 'btn btn-primary' %>

<% end %>

